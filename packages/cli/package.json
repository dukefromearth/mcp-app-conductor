{
  "name": "@mcp-app-conductor/cli",
  "version": "0.1.0",
  "description": "CLI for running conductor demos and host workflows",
  "type": "module",
  "bin": {
    "mcp-canvas": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node ./dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mcp-app-conductor/contracts": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.25.2",
    "mcp-app-conductor": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
